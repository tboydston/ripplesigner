{
  "name": "ripplesigner",
  "version": "1.0.0",
  "description": "Simple CLI based signer for XRP for offline signing. Accepts JSON transaction data and returns signed transaction for online broadcasting.",
  "main": "sign.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "XRP",
    "ripple",
    "sign",
    "transaction"
  ],
  "author": "Tracey Boydston",
  "license": "ISC",
  "dependencies": {
    "ripple-lib": "^1.3.3"
  }
}
